apiVersion: "k8s.cni.cncf.io/v1"
kind: NetworkAttachmentDefinition
metadata:
  name: {{ ipsec_name }}-lan
spec:
  config: '{
      "cniVersion": "0.3.0",
      "type": "macvlan",
      "master": "{{ lan_iface_name }}",
      "mode": "bridge",
      "ipam": {
        "type": "static",
	  "addresses": [
		{
		        "address": "{{ pod_lan_ip }}"
		}
		]
	}    
}'